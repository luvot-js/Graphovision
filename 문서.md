# GraphoVision — 서비스 기획(PRD) & UI/UX 디자인 스펙 통합 문서

> 버전: v2.0 | 최종 수정: 2026-05-28
> 대상: 프론트엔드 개발자, 백엔드 개발자, UI/UX 디자이너
> 변경 이력: v1.0 → v2.0 심리 지표 8개 → 5개로 축소 (LABEL_DECISION.md 참고)

---

## 목차

1. [서비스 개요](#1-서비스-개요)
2. [비즈니스 모델 & 재화 시스템](#2-비즈니스-모델--재화-시스템)
3. [AI 모델 스펙](#3-ai-모델-스펙)
4. [정보 구조도 (IA)](#4-정보-구조도-ia)
5. [디자인 시스템](#5-디자인-시스템)
6. [페이지별 기획 & UI 스펙](#6-페이지별-기획--ui-스펙)
7. [레이더 차트 스펙](#7-레이더-차트-스펙)
8. [API 연동 개요](#8-api-연동-개요)
9. [현재 구현 현황](#9-현재-구현-현황)

---

## 1. 서비스 개요

| 항목 | 내용 |
|------|------|
| 서비스명 | GraphoVision |
| 한줄 정의 | 내 필체로 읽는 나의 심리 — AI 필기 성격 분석 웹 서비스 |
| 핵심 기능 | 필기 이미지 업로드 → CNN 5지표 분석 → LLM 맞춤 리포트 생성 |
| 타겟 유저 | 심리 테스트에 흥미 있는 10~30대 MZ 세대 |
| 플랫폼 | 반응형 웹 (모바일 우선, max-width 480px) |
| 기술 스택 | React + TypeScript + Vite / Python + PyTorch |

### 핵심 서비스 플로우

```
회원가입 / 로그인
    ↓
필기 이미지 업로드 (Test 페이지)
    ↓
AI 분석 실행 (1 크레딧 차감)
    ↓
5각 레이더 차트 + LLM 리포트 (Result 페이지)
    ↓
친구와 궁합 비교 (Compatibility 페이지)
또는 과거 기록 조회 (MyPage)
```

---

## 2. 비즈니스 모델 & 재화 시스템

### 크레딧 구조

| 패키지 | 크레딧 | 가격 | 할인율 | 뱃지 |
|--------|--------|------|--------|------|
| 1회권 | 1 cr | 1,000원 | — | — |
| 5회권 | 5 cr | 4,500원 | 10% | 인기 |
| 10회권 | 10 cr | 8,000원 | 20% | 추천 |
| 20회권 | 20 cr | 14,000원 | 30% | 30% 할인 |

- 검사 1회 실행 시 **1 크레딧 자동 차감**
- 크레딧 잔액 0 → 검사 시도 시 충전 페이지로 이동 유도
- 결제 수단: 토스페이먼츠, 카카오페이

### 크레딧 부족 플로우

```
검사 시도
  ↓
크레딧 잔액 확인
  ├── 잔액 있음 → 검사 진행
  └── 잔액 없음 → "크레딧이 부족합니다" 모달
                   → [충전하기] 버튼 → /billing
```

---

## 3. AI 모델 스펙

### 3-1. 모델 아키텍처

| 항목 | 내용 |
|------|------|
| 백본 | ResNet (GraphoVisionResNet) |
| 출력 | 5개 레이블 (멀티레이블 이진 분류) |
| Dropout | 0.3 |
| 손실 함수 | Focal Loss (클래스 불균형 대응) |

### 3-2. 학습 전략 (2단계)

**1단계 — Warm-up (10 epoch)**
- 백본 파라미터 동결 (freeze)
- FC head만 학습
- Learning Rate: 1e-3

**2단계 — Fine-tuning (나머지 epoch)**
- 전체 파라미터 학습 해제
- Learning Rate: 5e-5

**공통 설정**

| 항목 | 값 |
|------|---|
| Batch Size | 32 |
| Total Epochs | 70 |
| Optimizer | Adam (weight_decay=1e-4) |
| LR Scheduler | ReduceLROnPlateau (patience=5, factor=0.5) |
| Early Stopping | patience=15 |
| Sampler | WeightedRandomSampler (클래스 불균형 대응) |

### 3-3. 데이터셋

| 구분 | 샘플 수 |
|------|--------|
| 전체 | 13,156 |
| Train | 9,693 |
| Validation | 1,643 |
| Test | 1,820 |

- 입력: IAM Handwriting Database (lines/sentences 단위)
- 레이블: HBPA label_list.txt (7~14번째 열 중 5개 선택)
- 전처리: 비율 유지 Height 224px 리사이즈 → Random Crop 224×224

### 3-4. 5가지 심리 지표 (최종 확정)

> 기존 8개 지표 중 데이터 부족 및 학습 불가로 판단된 3개(Imagination, Fear/Anxiety, Introversion)를 제외.  
> 제외 근거: LABEL_DECISION.md 참고.

| # | 레이블 | 한국어 | 양성 비율 | 최적 Threshold | 최종 F1 |
|---|--------|--------|-----------|---------------|---------|
| 0 | Emotional Stability | 정서 | 47.9% | 0.10 | ~0.67 |
| 1 | Social Behavior | 사회성 | 91.9% | 0.50 | ~0.96 |
| 2 | Mental Energy | 에너지 | 66.9% | 0.40 | ~0.82 |
| 3 | Willpower | 의지력 | 23.2% | 0.40 | ~0.44 |
| 4 | Sensitivity | 감수성 | 43.4% | 0.50 | ~0.66 |

**평균 정확도: 69.0% / 최적 Threshold 적용 평균 F1: 70.5%**

### 3-5. 제외된 지표 (3개)

| 레이블 | 양성 수 | 양성 비율 | 제외 이유 |
|--------|--------|-----------|---------|
| Imagination | 471 | 3.6% | 절대적 데이터 부족, F1 0.11~0.18 수준으로 학습 불가 |
| Fear / Anxiety | 752 | 5.7% | 4단계 실험 전체에서 개선 없음 |
| Introversion / Extroversion | 855 | 6.5% | Threshold 탐색 결과 0.50 수렴 — 확률 분포 학습 실패 |

---

## 4. 정보 구조도 (IA)

```
GraphoVision
│
├── 비로그인 접근 가능
│   ├── /              랜딩 페이지
│   ├── /login         로그인
│   └── /signup        회원가입
│
└── 로그인 필요
    ├── /test                    필기 업로드 & 검사 실행
    ├── /result/:id              검사 결과
    ├── /compatibility           궁합 분석
    ├── /billing                 크레딧 충전
    └── /mypage
        └── /mypage/history/:id  과거 결과 상세
```

---

## 5. 디자인 시스템

### 5-1. 컬러 팔레트

| 역할 | 이름 | HEX | 사용처 |
|------|------|-----|--------|
| Background | Cream White | `#FAF8F5` | 전체 배경 |
| Surface | Warm Beige | `#F0EBE3` | 카드, 패널 |
| Border | Warm Gray | `#D6CFC7` | 구분선, 인풋 테두리 |
| Text Primary | Charcoal | `#2D2926` | 본문, 제목 |
| Text Secondary | Warm Brown | `#7A6A5A` | 설명, 부제목 |
| Primary | Indigo | `#4F46E5` | CTA 버튼, 강조 |
| Primary Hover | Deep Indigo | `#4338CA` | 버튼 hover |
| Success | Sage Green | `#4CAF50` | 높은 점수, 완료 상태 |
| Warning | Amber | `#F59E0B` | 중간 점수, 경고 |
| Danger | Coral Red | `#EF4444` | 낮은 점수 |
| Radar A | Indigo Blue | `#3B82F6` | 나의 레이더 차트 |
| Radar B | Coral Red | `#EF4444` | 친구 레이더 차트 |

### 5-2. 타이포그래피

```
Font Family: 'Pretendard', 'Inter', sans-serif

H1 (Title):    28px / Bold    / Charcoal
H2 (SubTitle): 22px / SemiBold / Charcoal
H3 (Section):  18px / SemiBold / Charcoal
Body:          15px / Regular  / Charcoal
Caption:       13px / Regular  / Warm Brown
Button:        15px / SemiBold
```

### 5-3. 공통 컴포넌트

**Button**
```
[Primary]   bg: Indigo / text: White / radius: 12px / padding: 14px 28px
[Secondary] bg: Transparent / border: 1.5px Indigo / text: Indigo
[Disabled]  bg: #E5E7EB / text: #9CA3AF
```

**Card**
```
bg: White / border: 1px #E5E7EB / border-radius: 16px
padding: 24px / box-shadow: 0 2px 12px rgba(0,0,0,0.06)
```

**Input**
```
bg: White / border: 1.5px #D6CFC7 / border-radius: 10px
padding: 12px 16px / focus-border: Indigo
```

### 5-4. 지표 점수 바 컬러 기준

```
0.0 ~ 0.3 : #EF4444 (낮음 — Coral Red)
0.3 ~ 0.6 : #F59E0B (중간 — Amber)
0.6 ~ 1.0 : #4CAF50 (높음 — Sage Green)
```

### 5-5. 레이아웃

- 기본 그리드: max-width `480px` (모바일 우선), 데스크탑 센터 정렬
- 섹션 간격: `32px`
- 컴포넌트 간격: `16px`
- 화면 좌우 패딩: `20px`

---

## 6. 페이지별 기획 & UI 스펙

---

### 6-1. 랜딩 페이지 (`/`)

**목적**: 비로그인 사용자 대상 서비스 소개 및 진입점

**유저 시나리오**
```
1. 사용자가 / 진입
2. 서비스 소개 확인
3. [시작하기] → /login 또는 [회원가입] → /signup
```

**UI 구성**

| 컴포넌트 | 설명 |
|----------|------|
| 로고 + 슬로건 | "필기체로 알아보는 나의 성격" |
| 기능 소개 카드 (4개) | 필체 분석 / 성격 파악 / 궁합 분석 / 히스토리 관리 |
| CTA 버튼 | [시작하기] Primary / [회원가입] Secondary |

**애니메이션**: Framer Motion `opacity + scale` 진입 효과

---

### 6-2. 로그인 / 회원가입 (`/login`, `/signup`)

**목적**: 이메일/비밀번호/닉네임 기반 자체 인증 (소셜 로그인 없음)

**유저 시나리오 — 로그인**
```
1. 이메일 + 비밀번호 입력
2. [로그인] 클릭 → 성공 시 /test 이동, 크레딧 세션 로드
3. 실패 시 인라인 에러 메시지 표시
```

**유저 시나리오 — 회원가입**
```
1. 이메일 / 닉네임 / 비밀번호 / 비밀번호 확인 입력
2. [가입하기] 클릭 → 성공 시 /login 이동
```

**입력 검증 규칙**

| 필드 | 규칙 |
|------|------|
| 이메일 | RFC 이메일 형식 |
| 닉네임 | 2~10자, 특수문자 제외 |
| 비밀번호 | 8자 이상, 영문+숫자 혼합 |
| 비밀번호 확인 | 비밀번호 일치 여부 실시간 표시 |

**UI 컴포넌트**

| 컴포넌트 | 스펙 |
|----------|------|
| EmailInput | type=email, 자동완성 on |
| PasswordInput | type=password, 표시/숨기기 토글 |
| ErrorMessage | 인라인, Coral Red 텍스트 |
| SubmitButton | Primary, 전체 너비 |

---

### 6-3. 검사 페이지 (`/test`)

**목적**: 필기 이미지 업로드 및 AI 분석 실행 — 서비스의 핵심 진입점

**3단계 플로우**

```
Step 1: select    분석 대상 선택 (기존 인물 or 신규 추가)
   ↓
Step 2: upload    필기 이미지 업로드 + 분석 시작
   ↓
Step 3: analyzing 분석 중 로딩 화면 → /result/:id 이동
```

**Step 1 — 분석 대상 선택**

- 기존 검사 내역 리스트 (선택 가능)
- "새로운 사람" 텍스트 입력 필드
- [다음] 버튼

**Step 2 — 이미지 업로드**

| 컴포넌트 | 스펙 |
|----------|------|
| DropZone | 점선 테두리, 드래그 오버 시 Indigo 테두리로 변경 |
| CameraButton | Secondary, `input[capture]` 활용 |
| ImagePreview | 썸네일 + [X] 제거 버튼 |
| TipCard | 권장 필기 조건 안내 |
| AnalyzeButton | Primary, "분석 시작 (1 크레딧)" |
| CreditBadge | TopNav 우상단, 클릭 시 /billing |

**권장 필기 조건 안내**
```
✅ 흰 종이에 검정/파란 펜
✅ 자연광 또는 밝은 환경
✅ 최소 A5 크기 분량의 필기
❌ 타이핑 텍스트 불가
```

**Step 3 — 분석 중 (Loading Overlay)**

```
전체 화면 오버레이
회전 Spinner
"{이름}님의 필체를 분석하고 있어요"
"잠시만 기다려 주세요..."
Progress Bar (0 → 100%)
"필압 패턴 분석 중..." (단계별 텍스트)
```

---

### 6-4. 결과 페이지 (`/result/:id`)

**목적**: CNN 5지표 분석 결과 시각화 + LLM 맞춤 리포트 제공

**유저 시나리오**
```
1. 분석 완료 후 /result/:id 자동 이동
2. 5각 레이더 차트 애니메이션으로 등장 (0 → 실제값, 600ms)
3. 지표별 점수 바 순차 표시 (각 100ms 지연)
4. LLM 리포트 타이핑 효과로 스트리밍 (30ms/글자)
5. 공유 / 재검사 / 궁합 비교 액션
```

**UI 구성**

| 영역 | 컴포넌트 | 스펙 |
|------|----------|------|
| 차트 | 5각 RadarChart | Recharts, height=300, stroke=#4F46E5, fillOpacity=0.25 |
| 점수 바 | TraitScoreBar × 5 | 지표명 + Progress Bar + 소수 2자리 |
| 리포트 | LLMReportCard | Card + 타이핑 커서 애니메이션 |
| 공유 | 버튼 3개 | 카카오 공유(노란색) / 링크 복사 / 이미지 저장 |
| 액션 | 버튼 2개 | [다시 검사하기] Secondary → /test / [친구와 비교하기] Primary → /compatibility |

**5가지 지표 (레이더 차트 순서)**

```
정서 → 사회성 → 에너지 → 의지력 → 감수성
```

---

### 6-5. 궁합 페이지 (`/compatibility`)

**목적**: 두 사람의 필기 분석 결과를 비교하여 성격적 조화도 시각화

**2단계 플로우**

```
Phase 1: 두 사람 선택
  → 첫 번째 사람 선택 (기존 분석 내역 중)
  → 두 번째 사람 선택 (첫 번째 제외)
  → [궁합 분석 시작] 버튼

Phase 2: 결과 표시
  → 오버레이 레이더 차트 (두 사람 동시 표시)
  → 전체 조화도 점수
  → 시너지 분석 / 주의 포인트
```

**오버레이 레이더 차트 스펙**

```
나    : stroke=#3B82F6 (Indigo Blue), fillOpacity=0.2
친구  : stroke=#EF4444 (Coral Red),  fillOpacity=0.2
```

**궁합 점수 계산 로직 (기획)**

```
전체 조화도 = (1 - mean(|나의 지표 - 친구 지표|)) × 100
시너지 항목 : 두 사람 모두 0.7 이상인 지표
주의 항목   : 두 사람 차이가 0.4 이상인 지표
```

**UI 컴포넌트**

| 컴포넌트 | 스펙 |
|----------|------|
| PersonSelector | 체크마크 + Indigo 테두리 선택 카드 |
| OverlayRadarChart | 두 Radar 데이터셋 동시 표시 |
| CompatibilityScore | 전체 조화도 % + Progress Bar |
| SynergyCard | 초록 좌측 border, 💡 시너지 내용 |
| WarningCard | 노랑 좌측 border, ⚠️ 주의 내용 |

---

### 6-6. 크레딧 충전 페이지 (`/billing`)

**목적**: 크레딧 패키지 선택 및 간편결제 실행

**유저 시나리오**
```
1. /billing 진입 (또는 잔액 부족 모달에서 유입)
2. 현재 잔액 확인
3. 패키지 선택 (라디오 카드)
4. 결제 수단 선택 (토스페이먼츠 / 카카오페이)
5. [결제하기] 클릭 → 결제창 오픈
6. 결제 성공 → 크레딧 업데이트 → 성공 모달
7. 결제 실패 → 실패 모달 (재시도 가능)
```

**UI 컴포넌트**

| 컴포넌트 | 스펙 |
|----------|------|
| CurrentCreditDisplay | Indigo 배경, 보유 크레딧 강조 |
| PackageCard | 라디오 카드, 선택 시 Indigo 테두리 |
| DiscountBadge | "인기", "추천" Indigo 뱃지 |
| PaymentMethodSelector | 토스 / 카카오 로고 버튼 토글 |
| PayButton | Primary, 선택된 금액 동적 표시 |
| SecurityNote | 🔒 안전한 결제 보장 문구 |

---

### 6-7. 마이페이지 (`/mypage`)

**목적**: 프로필 확인, 크레딧 잔액 조회, 과거 검사 기록 열람

**UI 구성**

| 영역 | 컴포넌트 | 스펙 |
|------|----------|------|
| 프로필 | ProfileCard | Avatar + 닉네임 + 이메일 + 크레딧 잔액 + [충전하기] 링크 |
| 히스토리 | HistoryListItem | 날짜 + 미니 레이더 썸네일(60×60px) + 평균 점수 + [→] |
| 더 보기 | PaginationButton | 추가 로드 |
| 로그아웃 | LogoutButton | 하단 텍스트 버튼 → /login |

### 6-8. 히스토리 상세 (`/mypage/history/:id`)

Result 페이지(`/result/:id`)와 동일한 레이아웃 재사용.

- 상단: 날짜 레이블 표시 ("2026.05.23 검사 기록")
- 공유 버튼 및 크레딧 차감 없음 (순수 열람 전용)

---

## 7. 레이더 차트 스펙

### 사용 라이브러리: Recharts

### 5각 단일 차트 (Result, HistoryDetail)

```tsx
const TRAITS = ['정서', '사회성', '에너지', '의지력', '감수성'];

const data = scores.map((val, i) => ({
  name: TRAITS[i],
  value: Math.round(val * 100),
  fullMark: 100,
}));

<ResponsiveContainer width="100%" height={300}>
  <RadarChart data={data}>
    <PolarGrid stroke="#E5E7EB" />
    <PolarAngleAxis
      dataKey="name"
      tick={{ fontSize: 12, fill: '#7A6A5A' }}
    />
    <Radar
      dataKey="value"
      stroke="#4F46E5"
      fill="#4F46E5"
      fillOpacity={0.25}
      isAnimationActive={true}
      animationDuration={600}
    />
  </RadarChart>
</ResponsiveContainer>
```

### 오버레이 차트 (Compatibility)

```tsx
const mergedData = TRAITS.map((trait, i) => ({
  name: trait,
  [person1.name]: person1.scores[i],
  [person2.name]: person2.scores[i],
  fullMark: 100,
}));

<RadarChart data={mergedData}>
  <PolarGrid />
  <PolarAngleAxis dataKey="name" />
  <Radar dataKey={person1.name} stroke="#3B82F6" fill="#3B82F6" fillOpacity={0.2} />
  <Radar dataKey={person2.name} stroke="#EF4444" fill="#EF4444" fillOpacity={0.2} />
</RadarChart>
```

### 미니 썸네일 (MyPage 히스토리)

```tsx
<RadarChart width={60} height={60} data={data}>
  <PolarGrid />
  <Radar dataKey="value" stroke="#4F46E5" fill="#4F46E5" fillOpacity={0.3}
         isAnimationActive={false} />
</RadarChart>
```

### Windows + Docker 환경 Hot Reload 설정

```ts
// vite.config.ts
server: {
  watch: {
    usePolling: true,
    interval: 1000,
  },
}
```

---

## 8. API 연동 개요

### 엔드포인트 목록

| 엔드포인트 | 메서드 | 설명 |
|------------|--------|------|
| `/api/auth/signup` | POST | 회원가입 |
| `/api/auth/login` | POST | 로그인 → JWT 반환 |
| `/api/auth/logout` | POST | 로그아웃 |
| `/api/user/me` | GET | 프로필 + 크레딧 잔액 |
| `/api/analyze` | POST | 이미지 업로드 → 분석 실행 (1 cr 차감) |
| `/api/result/:id` | GET | 결과 조회 |
| `/api/history` | GET | 검사 기록 리스트 (페이지네이션) |
| `/api/history/:id` | GET | 특정 기록 상세 |
| `/api/compatibility` | POST | 두 결과 ID 비교 분석 |
| `/api/billing/order` | POST | 결제 주문 생성 |
| `/api/billing/confirm` | POST | 결제 승인 처리 |

### 분석 요청 / 응답 예시

**Request**
```
POST /api/analyze
Content-Type: multipart/form-data
Authorization: Bearer {token}

Body: { image: File }
```

**Response**
```json
{
  "resultId": "abc123",
  "scores": [0.82, 0.64, 0.71, 0.89, 0.78],
  "traits": ["정서", "사회성", "에너지", "의지력", "감수성"],
  "report": "당신의 필체에서는 강한 의지력과 높은 정서적 안정성이 느껴집니다..."
}
```

### 궁합 분석 응답 예시

```json
{
  "compatibilityScore": 87,
  "synergy": ["의지력"],
  "caution": ["감수성"],
  "summary": "두 사람의 의지력이 비슷해 강력한 팀이 될 수 있어요!"
}
```

---

## 9. 현재 구현 현황

### 프론트엔드 (GraphoVision_UI)

| 페이지 | 경로 | 구현 상태 | 비고 |
|--------|------|-----------|------|
| Landing | `/` | ✅ 완료 | |
| Login | `/login` | ✅ 완료 | Mock 인증 |
| Signup | `/signup` | ✅ 완료 | Mock 인증 |
| Test | `/test` | ✅ 완료 | Mock 분석 (3초 후 이동) |
| Result | `/result/:id` | ✅ 완료 | 5지표 레이더 차트 |
| Compatibility | `/compatibility` | ✅ 완료 | 5지표 오버레이 차트 |
| Billing | `/billing` | ✅ 완료 | Mock 결제 |
| MyPage | `/mypage` | ✅ 완료 | Mock 데이터 |
| HistoryDetail | `/mypage/history/:id` | ✅ 완료 | 5지표 레이더 차트 |

### 백엔드 / AI

| 항목 | 상태 | 비고 |
|------|------|------|
| 데이터 파이프라인 | ✅ 완료 | IAM + HBPA 연동 |
| 모델 학습 (train.py) | ✅ 완료 | best_model.pth 저장 |
| 모델 평가 (evaluate.py) | ✅ 완료 | 4종 시각화 자료 생성 |
| FastAPI 서버 | ⬜ 미구현 | API 연동 필요 |
| LLM 리포트 생성 | ⬜ 미구현 | 현재 하드코딩 텍스트 |
| 실제 결제 연동 | ⬜ 미구현 | 토스페이먼츠 / 카카오페이 SDK |
| 인증 시스템 | ⬜ 미구현 | JWT 발급 / 세션 관리 |

### Docker 실행 환경

```bash
# GraphoVision_UI 디렉토리에서 실행
docker-compose up --build -d

# 접속
http://localhost:5173
```

- Node.js 22 Alpine + pnpm@9
- Vite dev server (hot reload, polling 모드)
- 소스 변경 시 자동 반영

---

*GraphoVision PRD & UI Spec v2.0*
