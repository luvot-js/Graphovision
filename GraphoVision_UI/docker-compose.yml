services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      # 소스 변경 시 실시간 반영 (hot reload)
      - .:/app
      # node_modules는 컨테이너 것 유지
      - /app/node_modules
    environment:
      - NODE_ENV=development
